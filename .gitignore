# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyd
*.pyo

# ── 虚拟环境 ──────────────────────────────────────────────────────────
venv/
.venv/
env/
.env

# ── PyInstaller 产物 ──────────────────────────────────────────────────
build/
dist/

# ── 运行时数据（会话、笔记、日志）────────────────────────────────────
data/
crash.log
run_log.txt

# ── 用户配置（含 API Key，不入库）────────────────────────────────────
config/*
!config/app_config.example.json

# ── IDE ───────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml

# ── AI 工具本地配置（个人开发环境，不入库）──────────────────────────
.claude/
.codex/
.agents/

nanobot/**
/nanobot
/logs

# ── 实验性子项目 / 临时工作区（虚拟环境、模型下载等，不入库）──────────
work/
